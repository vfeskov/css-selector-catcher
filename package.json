{
  "name": "css-selector-catcher",
  "version": "1.0.1",
  "description": "",
  "main": "app/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "if [[ ${MODE} == \"server\" ]]; then yarn --production; else yarn; fi",
    "start": "node app/index.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@reactivex/rxjs": "^5.0.0-rc.1",
    "cheerio": "^0.22.0",
    "nodemailer": "^2.6.4",
    "nodemailer-ses-transport": "^1.5.0",
    "rx-http-request": "^1.2.0"
  },
  "devDependencies": {
    "open": "0.0.5",
    "play-sound": "^1.1.1"
  }
}
